node_modules/
.next/
.env
.env.local
env.local

# Separately hosted repos — not embedded here to avoid broken gitlinks
inaya-mobile/
inaya-network-dapp/custody-sdk/

# Stray backups / archives, not real project content
inaya-network-dapp/custody-sdk.zip
inaya-network-dapp/src/app/page.js.bak-20260802

# Hardhat build output — regenerable via `npx hardhat compile`
artifacts/
cache/

# Portable desktop builds (scripts/build-portable-desktop.ps1) — large
# regenerable binaries, not source.
portable-builds/
